// Gerando um número inteiro aletório entre 0 e 10
const aleatorio = Math.round(Math.random() * 10)
console.log(aleatorio)

// Gerando a raiz quadrada de um número
console.log(169 ** (1/2))

// Número dividido por zero gera infinity(booleano)
console.log(!!(8/0))

let num = 8.74
let n = Math.floor(num) // arredonda para baixo
let n1 = Math.ceil(num) // arredonda para cima
let n2 = Math.round(num) // arredonda para o mais próximo
let n3 = Math.max(10, 5, 2, 87, -5) // retorna o maior número
let n4 = Math.min(10, 5, 2, 87, -5) // retorna o menor número
let n5 = Math.random() // gera um número aleatório entre 0 e 1

console.log(n5)